package com.anismhub.ticketsystem.presentation.components

enum class TabItem(
    val title: String
) {
    Open(title = "Open"),
    OnProgress(title = "On Progress"),
    Closed(title = "Closed"),
}
